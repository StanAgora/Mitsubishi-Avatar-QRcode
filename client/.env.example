# Agora App ID used by the browser SDK to join the RTC channel.
VITE_AGORA_APP_ID=your_agora_app_id

# Override only if the API is served from a different origin than the web app.
# VITE_API_BASE_URL=http://localhost:8080/api
